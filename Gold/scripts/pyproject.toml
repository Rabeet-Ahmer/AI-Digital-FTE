[project]
name = "gold-watcher"
version = "0.3.0"
description = "Gold-tier AI Employee: file watcher, orchestrator, LinkedIn, Meta, Twitter, and Odoo integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "watchdog>=6.0.0",
    "playwright>=1.40.0",
    "requests>=2.31.0",
    "requests-oauthlib>=1.3.1",
]

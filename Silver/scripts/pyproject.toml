[project]
name = "silver-watcher"
version = "0.2.0"
description = "Silver-tier AI Employee: file watcher, orchestrator, and LinkedIn automation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "watchdog>=6.0.0",
    "playwright>=1.40.0",
]
